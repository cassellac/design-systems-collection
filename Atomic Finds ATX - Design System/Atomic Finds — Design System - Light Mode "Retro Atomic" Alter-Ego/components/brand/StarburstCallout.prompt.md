One-line: the brand's grid-breaking promo graphic — a spiky vector starburst wrapping short, punchy display text.

```jsx
<StarburstCallout size={180}>2 FONT STYLES!</StarburstCallout>
<StarburstCallout color="var(--rust-500)" rotate={6}>-30%</StarburstCallout>
```

Keep the copy tiny and loud (2–4 words). Tune `points` for more/fewer spikes and `rotate` for tilt. Static decoration — never animate it.
