One-line: a text field with an uppercase eyebrow label and a heavy ink border that lifts into an offset shadow on focus.

```jsx
<Input label="Search the crates" placeholder="Artist, label, year…" />
<Input label="Email" type="email" error />
```

Pass `iconLeft` for a leading glyph, `error` for cherry border, `disabled` to mute.
