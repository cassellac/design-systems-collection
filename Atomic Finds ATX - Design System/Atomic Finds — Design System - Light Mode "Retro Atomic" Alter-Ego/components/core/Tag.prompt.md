One-line: a small, quiet filter/metadata tag — lighter than Badge — that fills with ink when active.

```jsx
<Tag>Soul-Jazz</Tag>
<Tag active onClick={toggle}>Hard Bop</Tag>
```

Use for genre facets and filter chips. Provide `onClick` to make it selectable; `active` fills it ink.
