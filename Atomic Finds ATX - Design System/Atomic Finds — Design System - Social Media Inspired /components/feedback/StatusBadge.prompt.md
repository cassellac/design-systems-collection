# StatusBadge
The pop-art SOLD / AVAILABLE / NEW DROP sticker used on product photos and cards.

```jsx
<StatusBadge status="available" />
<StatusBadge status="sold" size="sm" />
```

Variants: `status` = `available` | `sold` | `new-drop`. `size` = `sm` | `md` (default).
